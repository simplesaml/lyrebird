# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# ruby-saml requires base64 and logger without declaring either; base64 is
# not a default gem as of Ruby 3.4, nor logger as of Ruby 4.0.
gem "base64"
gem "logger"
gem "minitest"
gem "rake"
gem "ruby-saml"
